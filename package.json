{
  "name": "demo-app",
  "version": "1.0.0",
  "scripts": {
    "setup": "rm -rf build/ dist/ .cache/ && mkdir -p build && cp config/app.config.template.js config/app.config.js",
    "dev": "node src/index.js",
    "start": "node src/index.js"
  },
  "dependencies": {
    "express": "^4.18.0"
  }
}
